/**
 * Map data item members to corresponding HB HTTP message
 * shape
 */
export function encodeDataItem({ processId, data, tags, anchor }: {
    processId: any;
    data: any;
    tags: any;
    anchor: any;
}): Promise<{
    headers: Headers;
    body: Blob;
}>;
export function httpSigName(address: any): string;
export function processIdWith({ logger: _logger, signer, HB_URL }: {
    logger: any;
    signer: any;
    HB_URL: any;
}): (fields: any) => any;
export function requestWith({ fetch, logger: _logger, HB_URL, signer }: {
    fetch: any;
    logger: any;
    HB_URL: any;
    signer: any;
}): (fields: any) => any;
export function deployProcessWith({ fetch, logger: _logger, HB_URL, signer }: {
    fetch: any;
    logger: any;
    HB_URL: any;
    signer: any;
}): (args: any) => any;
export function deployMessageWith({ fetch, logger: _logger, HB_URL, signer }: {
    fetch: any;
    logger: any;
    HB_URL: any;
    signer: any;
}): (args: any) => any;
export function loadResultWith({ fetch, logger: _logger, HB_URL, signer }: {
    fetch: any;
    logger: any;
    HB_URL: any;
    signer: any;
}): (args: any) => any;
/**
 * @typedef Env3
 * @property {fetch} fetch
 * @property {string} CU_URL
 *
 * @typedef QueryResultsArgs
 * @property {string} process - the id of the process being read
 * @property {string} from - cursor to start the list of results
 * @property {string} to - cursor to stop the list of results
 * @property {string} sort - "ASC" or "DESC" to describe the order of list
 * @property {number} limit - the number of results to return
 *
 * @callback QueryResults
 * @param {QueryResultsArgs} args
 * @returns {Promise<Record<string, any>}
 *
 * @param {Env3} env
 * @returns {QueryResults}
 */
export function queryResultsWith({ fetch, HB_URL, logger, signer }: Env3): QueryResults;
export function relayerWith({ fetch, logger, HB_URL, signer }: {
    fetch: any;
    logger: any;
    HB_URL: any;
    signer: any;
}): (url: any, options: any) => Promise<any>;
export class InsufficientFunds extends Error {
}
export class RedirectRequested extends Error {
}
export type Env3 = {
    fetch: typeof globalThis.fetch;
    CU_URL: string;
};
export type QueryResultsArgs = {
    /**
     * - the id of the process being read
     */
    process: string;
    /**
     * - cursor to start the list of results
     */
    from: string;
    /**
     * - cursor to stop the list of results
     */
    to: string;
    /**
     * - "ASC" or "DESC" to describe the order of list
     */
    sort: string;
    /**
     * - the number of results to return
     */
    limit: number;
};
export type QueryResults = (args: QueryResultsArgs) => Promise<Record<string, any>>;
